# Todos/Features missing
## Completed recently
- Added `knowledge-cli quickstart` to scaffold defaults and initialize the DB.
- Added streamlined defaults for `--db` and `--notes-root` across common commands.
- Added positional lookup: `knowledge-cli get <entity>`.
- Added explicit field flags for capture commands: `--slug` and `--body`.
- Added `knowledge-cli completions <shell>` for shell self-discovery.
- Added short binary alias `kno` alongside `knowledge-cli`.
- Improved CLI error messages with one-line fix examples.
- Added tag-based GitHub Actions release workflow for Ubuntu and macOS artifacts.
