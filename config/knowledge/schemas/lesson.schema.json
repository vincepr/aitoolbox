{
    "$id": "https://aitoolbox/schemas/lesson.v1.json",
    "type": "object",
    "additionalProperties": false,
    "required": ["$schema", "slug", "body"],
    "properties": {
        "$schema": {"const": "https://aitoolbox/schemas/lesson.v1.json"},
        "slug": {"type": "string", "minLength": 1},
        "body": {"type": "string", "minLength": 1}
    }
}
